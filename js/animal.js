function toggle() {
  var audioElem = document.getElementById('sound1');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
} 
function duck() {
  var audioElem = document.getElementById('sound2');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
//Deer calls//
function buck() {
  var audioElem = document.getElementById('sound3');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function deer() {
  var audioElem = document.getElementById('sound4');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function deer2() {
  var audioElem = document.getElementById('sound5');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
//Elk Calls//
function elk() {
  var audioElem = document.getElementById('sound6');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}

function elkcall() {
  var audioElem = document.getElementById('sound7');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function cow() {
  var audioElem = document.getElementById('sound8');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function moose() {
  var audioElem = document.getElementById('sound9');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
// Wolf calls 
function wolf() {
  var audioElem = document.getElementById('sound10');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
$(".hound").on('click',function(){
  var audioElem = document.getElementById('sound17');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
});
//bear calls//
function bear() {
  var audioElem = document.getElementById('sound11');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function bears() {
  var audioElem = document.getElementById('sound12');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
//varmint//
function bunny() {
  var audioElem = document.getElementById('sound13');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function yip() {
  var audioElem = document.getElementById('sound14');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function lone() {
  var audioElem = document.getElementById('sound18');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function fox() {
  var audioElem = document.getElementById('sound15');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
function foxhurt() {
  var audioElem = document.getElementById('sound16');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
}
$(".fight").on('click',function(){
  var audioElem = document.getElementById('sound19');
  if (audioElem.paused)
    audioElem.play();
  else
    audioElem.pause();
});

//playlist//
